# wrangler.toml 必须入库：D1 绑定写在里面，
# 否则每次 Git 部署会用空配置覆盖 Dashboard 手动加的绑定（表现为"绑定不了/绑了就掉"）。
# 线上值（database_id）不是敏感凭据，可安全入库。

# 图片压缩中间产物（待上传 R2，用完即删，不入库）
webp-output/

# 本地临时脚本/诊断产物
.tmp/

# 本地临时脚本/诊断产物
.tmp/
