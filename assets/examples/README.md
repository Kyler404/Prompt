# Template Example Images

Put local example images in this folder, then reference them from a template with a relative path like:

```text
./assets/examples/example.jpg
```

Supported browser image formats such as JPG, PNG, WebP, and GIF work here. The image file is not embedded in JSON; the template only stores the path, title, and short description.
